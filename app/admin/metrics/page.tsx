import { listAdminResource } from "@/lib/admin";
import { readableValue } from "@/lib/admin-types";
import { AdminNotice } from "../admin-ui";
export default async function MetricsPage() { const result = await listAdminResource("admin_get_metrics"); const row = result.rows[0] ?? {}; return <><h1 className="text-2xl font-bold tracking-tight text-slate-950">Métricas</h1><AdminNotice error={result.error} /><p className="mt-2 text-sm text-slate-600">Os sinais essenciais para acompanhar o MVP, sem transformar o painel em um BI.</p><section className="mt-7 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{Object.entries(row).map(([key, value]) => <div key={key} className="rounded-xl border border-slate-200 bg-white p-4"><p className="text-2xl font-bold text-slate-950">{readableValue(value)}</p><p className="mt-1 text-sm text-slate-600">{key.replaceAll("_", " ")}</p></div>)}</section>{result.rows.length === 0 && !result.error && <p className="mt-7 rounded-xl border border-dashed border-slate-300 bg-white p-8 text-center text-sm text-slate-500">Ainda não há métricas para mostrar.</p>}</>; }
